// /** @type {import('next').NextConfig} */
// const nextConfig = {};
// // next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.leetcode.com'], // Add the domain here
  },
}

export default nextConfig;
